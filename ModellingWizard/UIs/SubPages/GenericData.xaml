<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="ModellingWizard.UIs.SubPages.GenericData"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModellingWizard.UIs.SubPages"
    xmlns:controls = "using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:CommunityToolkit.WinUI.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid Background="White">
                <TextBlock Text="Added gernerics" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Button Content="Add Role Class" Name="AddRoleClassButton" Click="AddRoleClassButton_Click" HorizontalAlignment="Right" Visibility="Visible" Margin="0,0,10,0"/>
            </Grid>

            <TreeView Grid.Row="1">

            </TreeView>
        </Grid>
        <controls:DataGrid Grid.Column="1">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Atrribute"/>
                <controls:DataGridTextColumn Header="Value"/>
                <controls:DataGridTextColumn Header="Default" x:Name="DefaultGrid"/>
                <controls:DataGridTextColumn Header="Units" x:Name="UnitsGrid"/>
                <controls:DataGridTextColumn Header="DataType" x:Name="DataTypeGrid"/>
                <controls:DataGridComboBoxColumn Header="Semantic" x:Name="SemanticGrid"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
